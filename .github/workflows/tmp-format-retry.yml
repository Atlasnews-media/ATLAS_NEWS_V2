name: TMP format retry

on:
  push:
    branches: [fix/portada-indicadores-retry]

permissions:
  contents: write

jobs:
  format-retry:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: fix/portada-indicadores-retry
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Aplicar Prettier
        run: npx prettier --write src/components/MarketHistoryChart.astro src/pages/mercados/indices/index.astro scripts/sync-visual-locks.mjs
      - name: Commit si hay cambios
        shell: bash
        run: |
          if git diff --quiet; then
            echo "Sin cambios de formato"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/components/MarketHistoryChart.astro src/pages/mercados/indices/index.astro scripts/sync-visual-locks.mjs
          git commit -m "Format: normalizar cambios para CI"
          git push origin HEAD:fix/portada-indicadores-retry
