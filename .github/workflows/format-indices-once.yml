name: Format indices page once

on:
  push:
    branches: [feat/indices-mercado-v1]

permissions:
  contents: write

jobs:
  format:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: feat/indices-mercado-v1
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npx prettier --write src/pages/mercados/indices/index.astro src/pages/index.astro
      - name: Guardar formato
        run: |
          if git diff --quiet; then
            echo "Sin cambios."
            exit 0
          fi
          git config user.name "ATLAS NEWS Formatter"
          git config user.email "actions@users.noreply.github.com"
          git add src/pages/mercados/indices/index.astro src/pages/index.astro
          git commit -m "style: format indices page"
          git push origin HEAD:feat/indices-mercado-v1
