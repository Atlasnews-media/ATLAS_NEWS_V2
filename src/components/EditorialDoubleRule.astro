<div class="editorial-double-rule" aria-hidden="true"></div>

<style>
  .editorial-double-rule {
    display: none;
  }

  @media (max-width: 42rem) {
    .editorial-double-rule {
      display: block;
      width: 100%;
      height: 6px;
      margin: 0;
      padding: 0;
      border-top: 1px solid var(--rule);
      border-bottom: 1px solid var(--rule);
      box-sizing: border-box;
    }

    :global(.front-page > .editorial-double-rule) {
      height: 4px;
      border-top: 4px double var(--rule);
      border-bottom: 0;
    }

    :global(.front-page > .editorial-double-rule + .lead-grid) {
      margin-top: 0;
    }

    :global(.front-page .headline-board-title) {
      margin: 0;
      padding: 0.65rem 0;
      border-bottom: 1px solid var(--rule);
      color: var(--ink);
      font: 800 1.2rem/1 var(--sans);
      letter-spacing: 0.08em;
      text-transform: uppercase;
    }

    :global(.front-page .lead-grid > .editorial-double-rule) {
      height: 4px;
      border-top: 4px double var(--rule);
      border-bottom: 0;
    }

    :global(.front-page .lead-grid .edition-rail) {
      padding-top: 0;
      border-top: 0;
    }

    :global(.front-page .lead-grid .edition-rail .rail-title) {
      margin: 0;
      padding: 0.65rem 0;
      border-bottom: 1px solid var(--rule);
      color: var(--ink);
      font: 800 1.2rem/1 var(--sans);
      letter-spacing: 0.08em;
      text-transform: uppercase;
    }

    :global(.front-page .lead-grid) {
      border-bottom: 0;
    }
  }
</style>
